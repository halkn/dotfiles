# Global shell policy. shuck does not merge this file with a project config, so these values
# are repeated on the project side.

[lint]
select = ["google"]

[format]
indent-style = "space"
indent-width = 2
binary-next-line = true
switch-case-indent = true
