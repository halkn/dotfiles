# "Save view preferences?" rewrites this file, which the ~/.config symlink turns
# into a change to the repository. A repository-local .hunk/config.toml wins over
# it and is where that state belongs.
theme = "kanagawa-wave"
vcs = "git"
mode = "auto"
