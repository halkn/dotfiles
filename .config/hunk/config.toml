# Saving view preferences rewrites this file, which the ~/.config symlink makes a repo change.
prompt_save_view_preferences = false

theme = "kanagawa-wave"
vcs = "git"
mode = "auto"
agent_notes = true
color_moved = true
