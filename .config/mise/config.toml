[tools]
# terminal
starship = "latest"
"github:ogulcancelik/herdr" = "latest"

# cli
ripgrep = "latest"
fd = "latest"
fzf = "latest"
eza = "latest"
# NOTE: JSON/YAML の値確認は jq/yq から jaq (Rust) に統一。未導入時は各スクリプトが jq にフォールバックする。
jaq = "latest"
hyperfine = "latest"

# vcs
gh = "latest"
ghq = "latest"
delta = "latest"

# dev
uv = "latest"
node = "latest"

# editor
neovim = "nightly"
tree-sitter = "latest"

# LSP / Linter / Formatter
"pipx:ryl" = "latest"
rumdl = "latest"
# NOTE: yaml は ryl (lint + format) に統一。diffnav は codediff.nvim へ移行したため削除。
